import "./synthesis.css";

/** Synthesis keeps a small token bridge while using the shared Platform Web shell. */
export default function SynthesisLayout({ children }: { children: React.ReactNode }) {
  return children;
}
