{{{header "java"}}}
package {{application.packageName}}.exception;

import {{application.packageName}}.model.ErrorResponse;
import org.slf4j.MDC;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.ControllerAdvice;
import org.springframework.web.bind.annotation.ExceptionHandler;

{{#protectedRegion "api-contract" "java"}}
@ControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(IllegalArgumentException.class)
    public ResponseEntity<ErrorResponse> handleBadRequest(IllegalArgumentException ex) {
        return ResponseEntity.badRequest().body(
            new ErrorResponse("BAD_REQUEST", ex.getMessage(), MDC.get("correlationId"))
        );
    }

    @ExceptionHandler(UnsupportedOperationException.class)
    public ResponseEntity<ErrorResponse> handleNotImplemented(UnsupportedOperationException ex) {
        // Generated placeholder bodies throw this until the LLM patch lands.
        return ResponseEntity.status(501).body(
            new ErrorResponse("NOT_IMPLEMENTED", ex.getMessage(), MDC.get("correlationId"))
        );
    }

    @ExceptionHandler(Exception.class)
    public ResponseEntity<ErrorResponse> handleAny(Exception ex) {
        return ResponseEntity.status(500).body(
            new ErrorResponse("INTERNAL", ex.getMessage(), MDC.get("correlationId"))
        );
    }
}
{{/protectedRegion}}
