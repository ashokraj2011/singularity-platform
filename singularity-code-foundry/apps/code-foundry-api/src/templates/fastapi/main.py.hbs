{{{header "python"}}}
"""FastAPI application entry point for {{application.name}}."""
from fastapi import FastAPI
from .router import router

app = FastAPI(title="{{application.name}}", version="1.0.0")
app.include_router(router, prefix="{{api.basePath}}")
