# {{application.name}}

Generated by **Singularity Code Foundry** (Express + TypeScript variant).

| | |
|---|---|
| Spec hash    | `{{meta.specHash}}` |
| IR hash      | `{{meta.irHash}}` |
| Generator    | `{{meta.generatorVersion}}` |
| Template set | `{{meta.templateVersion}}` |

## Build + run

```
npm install
npm run dev
```

## Region markers

```
// <generated:protected region="...">    do not edit
// </generated:protected>

// <llm-editable region="...">            LLM patches may edit subject to ontology
// </llm-editable>
```

## Endpoint coverage

{{#each endpoints}}- `{{this.method}} {{this.fullPath}}` ({{this.operationId}}) — coverage **{{this.businessLogicCoverage}}**
{{/each}}
