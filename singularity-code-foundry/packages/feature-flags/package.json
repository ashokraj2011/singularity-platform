{
  "name": "@singularity-code-foundry/feature-flags",
  "version": "0.1.0",
  "private": true,
  "description": "Shared admin-gate client for the Singularity Code Foundry. Queries workgraph-api's /api/admin/feature-flags endpoint with a small per-key TTL cache and exposes isEnabled()/assertEnabled() helpers used by the CLI, REST, web UI, and any future BlueprintSession integration.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "engines": {
    "node": ">=20"
  }
}
