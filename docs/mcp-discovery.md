# Legacy Discovery Filename

This file name is kept only for old links. The Singularity component is now
documented as **Agent Execution Runtime**, not "MCP".

Use [Agent Execution Runtime Discovery](./runtime-discovery.md) for the current
documentation. The runtime still exposes legacy `/mcp/...` HTTP paths for
backward compatibility.
