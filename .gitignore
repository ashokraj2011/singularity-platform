# ── secrets ───────────────────────────────────────────────────────────────
.env
.env.bak*
.env.local
.env.*.local
.env.llm-secrets
# Laptop-side secrets for bin/laptop.sh (Copilot BYOK key, GitHub token).
# Committed template: .env.laptop.example
.env.laptop
# Generated by bin/use-artifactory.sh — may contain an Artifactory token
# (--inline mode). The committed template is .npmrc.artifactory.template.
.npmrc
**/.npmrc
.singularity/config.local.json
.singularity/ops-doctor.json
.singularity/llm-providers.json
.singularity/*.env
.singularity/*.sqlite
.singularity/secrets/
.singularity/keys/
.singularity/git-credentials
# M71 — operational audit log written by mcp-server's local sandbox-debug
# JSONL writer (M65 1C). Never source-controlled; rotates per workitem.
.singularity/mcp-audit.jsonl
.singularity/workitems/
.singularity/downloads/
.singularity/source-cache/
singularity-portal/public/ops-doctor.json
*.pem
*.key
*.p8
id_rsa
id_ed25519
/tmp/iam_jwt.txt

# ── node ──────────────────────────────────────────────────────────────────
node_modules/
.pnpm-store/
.npm/
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# ── build outputs ─────────────────────────────────────────────────────────
dist/
build/
.next/
out/
*.tsbuildinfo
# Vite's tsc-compiled config artefacts (the .ts source is the truth)
**/vite.config.js
**/vite.config.d.ts
**/vite.config.js.map

# ── prisma generated client (re-generated by `prisma generate`) ───────────
**/node_modules/.prisma/

# ── python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# ── databases / persisted state (recreated by docker volumes) ─────────────
*.db
*.db-journal
*.db-shm
*.db-wal
**/data/
pgdata/

# ── docker / minio data dirs ──────────────────────────────────────────────
**/minio_data/

# ── IDE / OS ──────────────────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp
*~

# ── logs ──────────────────────────────────────────────────────────────────
*.log
logs/

# ── claude session detritus ───────────────────────────────────────────────
.claude/projects/
.claude/sessions/
# keep launch.json + plans
!.claude/launch.json
!.claude/plans/
.singularity/llm-models.json
.singularity/mcp-models.json

# M29 — per-service Prisma client outputs (auto-generated by `prisma generate`).
agent-and-tools/apps/agent-runtime/generated/
agent-and-tools/apps/prompt-composer/generated/
mcp-server/.docker-sandbox/
.singularity/setup.conf
